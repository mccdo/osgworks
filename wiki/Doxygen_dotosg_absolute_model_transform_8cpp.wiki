#summary Documentation for the dotosgAbsoluteModelTransform.cpp file

= dotosgAbsoluteModelTransform.cpp =



== Functions ==

bool AMT_readLocalData(osg::Object & obj, osgDB::Input & fr)
  **
bool AMT_writeLocalData(const osg::Object & obj, osgDB::Output & fw)
  **
osgDB::RegisterDotOsgWrapperProxy AMT_Proxy(new  void, "AbsoluteModelTransform" void, "Object Node Transform AbsoluteModelTransform Group" void, AMT_readLocalData void, AMT_writeLocalData void)
  **
bool readMatrix(osg::Matrix & matrix, osgDB::Input & fr, const char * keyword)
  **
bool writeMatrix(const osg::Matrix & matrix, osgDB::Output & fw, const char * keyword)
  **

== Detailed Description ==




== Function Documentation ==

{{{
#bool AMT_readLocalData   (
  osg::Object & obj,
  osgDB::Input & fr  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#bool AMT_writeLocalData   (
  const osg::Object & obj,
  osgDB::Output & fw  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#osgDB::RegisterDotOsgWrapperProxy AMT_Proxy   (
  new  void,
  "AbsoluteModelTransform" void,
  "Object Node Transform AbsoluteModelTransform Group" void,
  AMT_readLocalData void,
  AMT_writeLocalData void  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#bool readMatrix   (
  osg::Matrix & matrix,
  osgDB::Input & fr,
  const char * keyword  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#bool writeMatrix   (
  const osg::Matrix & matrix,
  osgDB::Output & fw,
  const char * keyword  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

---
|| [Doxygen Main Page] || [Doxygen_files Files] ||

#summary Documentation for the Shapes.cpp file

= Shapes.cpp =



== Functions ==

unsigned int makeKey(const unsigned short a, const unsigned short b)
  **
void average3fv(float * r, const float * a, const float * b)
  **
bool buildGeodesicSphereData(const float radius, const unsigned int subdivisions, osg::Geometry * geom)
  **
bool buildAltAzSphereData(const float radius, const unsigned int subLat, const unsigned int subLong, osg::Geometry * geom, const bool wire)
  **
bool const buildCircleData(float radius, const unsigned int subdivisions, const osg::Vec3 & orientation, osg::Geometry * geom, const bool wire)
  **
void addPlaneData(const osg::Vec3 & corner, const osg::Vec3 & u, unsigned short uSteps, const osg::Vec3 & v, unsigned short vSteps, const osg::Vec3 & normal, osg::Geometry * geom)
  **
bool buildPlaneData(const osg::Vec3 & corner, const osg::Vec3 & u, const osg::Vec3 & v, const osg::Vec2s & subdivisions, osg::Geometry * geom)
  **
bool buildWirePlaneData(const osg::Vec3 & corner, const osg::Vec3 & u, const osg::Vec3 & v, const osg::Vec2s & subdivisions, osg::Geometry * geom)
  **
bool buildBoxData(const osg::Vec3 & halfExtents, const osg::Vec3s & subdivisions, osg::Geometry * geom)
  **
bool buildWireBoxData(const osg::Vec3 & halfExtents, osg::Geometry * geom)
  **
bool buildArrowData(osg::Geometry * geom)
  **

== Detailed Description ==




== Function Documentation ==

{{{
#unsigned int makeKey   (
  const unsigned short a,
  const unsigned short b  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#void average3fv   (
  float * r,
  const float * a,
  const float * b  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#bool buildGeodesicSphereData   (
  const float radius,
  const unsigned int subdivisions,
  osg::Geometry * geom  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#bool buildAltAzSphereData   (
  const float radius,
  const unsigned int subLat,
  const unsigned int subLong,
  osg::Geometry * geom,
  const bool wire  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#bool const buildCircleData   (
  float radius,
  const unsigned int subdivisions,
  const osg::Vec3 & orientation,
  osg::Geometry * geom,
  const bool wire  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#void addPlaneData   (
  const osg::Vec3 & corner,
  const osg::Vec3 & u,
  unsigned short uSteps,
  const osg::Vec3 & v,
  unsigned short vSteps,
  const osg::Vec3 & normal,
  osg::Geometry * geom  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#bool buildPlaneData   (
  const osg::Vec3 & corner,
  const osg::Vec3 & u,
  const osg::Vec3 & v,
  const osg::Vec2s & subdivisions,
  osg::Geometry * geom  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#bool buildWirePlaneData   (
  const osg::Vec3 & corner,
  const osg::Vec3 & u,
  const osg::Vec3 & v,
  const osg::Vec2s & subdivisions,
  osg::Geometry * geom  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#bool buildBoxData   (
  const osg::Vec3 & halfExtents,
  const osg::Vec3s & subdivisions,
  osg::Geometry * geom  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#bool buildWireBoxData   (
  const osg::Vec3 & halfExtents,
  osg::Geometry * geom  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

{{{
#bool buildArrowData   (
  osg::Geometry * geom  )
}}}
  
  

*Parameters*:
  a   And two parameters. 
  b   Here's the second. 
*Returns*:
  And a return value!

---
|| [Doxygen Main Page] || [Doxygen_files Files] ||
